To use this make a file called config.py and past the config file in,
then make a file called script.py and past the script.py file into it then you can code but leave the import at the top,
the config.py and script.py need to be in a folder. You can use VS code to run it.
